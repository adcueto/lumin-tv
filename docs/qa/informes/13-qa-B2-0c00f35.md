# Revisión independiente B2 — validación local y pendientes

Fecha: 2026-09-16. Repositorio: C:/Users/adcueto/Claude/lumin-tv.
Candidato: 0c00f35ea9b3163e24bcdeb1a9adc77f74373bde.
Base del bloque B2: ceadf1d2c66622c99d69389715ecb4ef60f8e468.

## Resultado

No se encontró un fallo funcional en los recorridos B2 ejecutados. Se requieren correcciones en la prueba de concurrencia y en el procedimiento de comparación con 6.9. No se emite APROBADO_QA global ni autorización de despliegue: los hallazgos Roku de B1 siguen pendientes, no se verificó CI y no se probó hardware.

El checkout permanece en main (ea610d6). Se inspeccionó el candidato mediante git show/diff; las referencias remotas son locales, sin fetch. Se copiaron cuatro archivos del candidato a revision-b2-0c00f35 dentro del espacio de QA para ejecutar pruebas aisladas. No se modificó el repositorio ni producción.

## Evidencia ejecutada

- git diff --check ceadf1d 0c00f35: sin errores.
- Diez funciones test_* de test_b2_higiene.py: diez pasan, ejecutadas directamente con sus aserciones originales. El entorno no tiene pytest: python -m pytest falla con No module named pytest. Para el ejecutor directo se retiró únicamente el import pytest sin uso, en memoria. Esto no equivale a una ejecución de pytest ni valida su descubrimiento o fixtures.
- Pruebas incluidas: sesiones y usuarios concurrentes, marcas de giro concurrentes, conexiones HTTP, 404 sin contador, descarga inicial frente a Range, índices de pantalla, bitácora, contrato de playlist y contrato de turnos.
- Comprobación adicional: 30 cierres de sesión y 30 altas concurrentes; las anteriores desaparecen, las nuevas permanecen y quedan las 31 sesiones esperadas.
- Comprobación adicional: adquisición de CANDADO seguida de crear_usuario y sus operaciones anidadas; termina dentro del plazo de diez segundos. No se reprodujo interbloqueo en ese recorrido; no demuestra ausencia de todos los interbloqueos posibles.
- Comprobación adicional: 120 peticiones paralelas a playlist; se recolectan explícitamente 120 resultados y los 120 son HTTP 200.
- Arnés: servidor HTTP real sobre loopback y datos sintéticos en carpetas temporales. No usa datos operativos.

No se reejecutaron las tres corridas informadas por Claude ni su comparación de siete fallos contra 6.9. Esos resultados siguen siendo evidencia aportada.

## Hallazgos de revisión

### B2-QA-01 [P2] La prueba de conexiones puede ocultar excepciones de trabajadores

Archivo del candidato: servidor/tests/test_b2_higiene.py, líneas 94–97.
La prueba descarta la lista de errores de _en_paralelo y comprueba all sobre los resultados recibidos sin exigir 120. Una excepción que alcance el recolector puede omitir un resultado y permitir un verde incompleto. Debe exigir errores vacíos, exactamente 120 resultados y todos HTTP 200. La comprobación adicional independiente sí obtuvo 120/120, por lo que este hallazgo afecta la fiabilidad de la regresión y no demuestra un fallo actual del servidor.

### B2-QA-02 [P2] El procedimiento de baseline no vuelve al servidor 6.9

Archivo del candidato: docs/coordinacion/12-entrega-B2-higiene.md, líneas 134–136.
git stash -- servidor_lumin.py no revierte un cambio ya comprometido en B2. Con el archivo limpio se seguiría probando 6.10; git stash pop podría además intentar aplicar un stash anterior ajeno a la prueba. Reemplazar el procedimiento por una copia aislada del servidor del SHA ea610d6 y ejecutar contra ella las mismas pruebas, conservando intacto el checkout. Registrar ambos SHA y resultados.

### Precisiones documentales

- En la línea 53, max(indice)+1 evita colisiones con índices existentes, pero no garantiza «nunca reutiliza»: eliminar el índice máximo permite reutilizarlo. Ajustar la descripción; no reasignar pantallas operativas dentro de B2.
- El script de caracterización original se detiene cuando falta la entrada estadística del 404. El documento aclara que los otros ocho casos se comprobaron con una copia ajustada. No describir ese 8/9 como ejecución completa del script sin cambios.
- Los contadores siguen midiendo solicitudes de descarga iniciales, no reproducciones confirmadas ni ventas.
- El plazo de reversión de un minuto es una estimación aportada, no una medición independiente.

## Pendientes heredados de B1

B2 no cambia archivos de roku-app. Permanecen los tres hallazgos estáticos de coordinacion/12-qa-B1-ceadf1d.md: presupuesto de caché no aplicado después de nuevas descargas, falta de reposición tras fallos con lista estable y descarte del primer comando válido posterior a reconexión. Requieren corrección y validación física posterior; no se presentan como fallos reproducidos en hardware.

## Orientación para el diseño B3

Es coherente preparar ahora el diseño PostgreSQL y su migración, manteniendo pendientes suscripciones, cobros, registro público y portal del propietario. La implementación actual B2 sigue siendo el servidor Python existente; todavía no aporta FastAPI, PostgreSQL ni React.

Solicitar antes de programar B3:

1. Esquema empresa/sucursal/usuarios/pantallas/medios/listas/turnos con propiedad, restricciones y mapeo desde cada JSON; conservar IDs y contratos Roku/POS.
2. Aislamiento de consultas y operaciones por empresa y permisos por sucursal. Las llaves compuestas protegen relaciones, pero no impiden por sí solas que una consulta lea otra empresa. Probar al menos dos empresas sintéticas, incluyendo medios y credenciales de pantallas.
3. Migración ensayable con respaldo, conteos e integridad; definir corte de escrituras y reversión que contemple datos nuevos después del cambio.
4. Migración de contraseñas y sesiones, compatibilidad temporal y decisión explícita sobre revocación.
5. Entrega de medios y autorización: si nginx sirve archivos, no debe saltarse el aislamiento previsto. Justificar almacenamiento local inicial frente al destino S3 compatible.
6. Si hay cola en PostgreSQL: transacciones, reclamación de trabajos, reintentos, idempotencia y recuperación tras caída del trabajador.
7. Identificar SHA y alcance del código reutilizado del proyecto SaaS pausado. Sus 36 pruebas reportadas no se han revisado aquí; requieren inspección y repetición en la integración real.

Preparar y revisar el diseño no autoriza migración ni despliegue de producción.
